({
    doInit : function(component, event, helper) {
        helper.setPathParts(component);
    },

    pathChanged : function(component, event, helper) {
        helper.setPathParts(component);
    }
})
